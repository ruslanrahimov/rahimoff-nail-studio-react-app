const SectionHeader = ({ children }) => {
  return (
    <h2 className="text-3xl font-light mb-10 max-md:text-center max-md:mb-6 max-md:text-2xl ">
      {children}
    </h2>
  );
};

export default SectionHeader;
